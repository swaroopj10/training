package com.fidelity.bowling;

public class Game {
	private int score = 0;
	
	public void rolls(int pins) {
		if(pins < 0 ) {
			throw new IllegalArgumentException();
		}
		score += pins;
	}
	
	public int getScore() {
		return score;
	}
}
